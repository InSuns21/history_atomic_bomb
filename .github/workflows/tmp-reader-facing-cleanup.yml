name: Temporary reader-facing achievement cleanup
on:
  push:
    branches: [audit-reader-facing-1945]
permissions:
  contents: write
jobs:
  audit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/tmp_reader_facing_cleanup.py
      - run: python scripts/build_site.py --check --strict-length --strict-tags
      - name: Commit audited catalog
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add ACHIEVEMENTS.md
          git diff --cached --quiet || git commit -m 'Rewrite achievement notes as reader-facing history'
          git push
